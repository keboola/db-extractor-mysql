{
  "parameters": {
    "id": 4,
    "enabled": true,
    "name": "incremental-lookback-late-commit",
    "outputTable": "in.c-main.incremental-lookback-late-commit",
    "incremental": true,
    "incrementalFetchingColumn": "seq",
    "incrementalFetchingLookback": "100",
    "primaryKey": ["id"],
    "table": {
      "schema": "test",
      "tableName": "incremental_lookback_late_commit"
    },
    "db": {
      "host": "%env(string:MYSQL_DB_HOST)%",
      "port": "%env(string:MYSQL_DB_PORT)%",
      "user": "%env(string:MYSQL_DB_USER)%",
      "#password": "%env(string:MYSQL_DB_PASSWORD)%",
      "database": "%env(string:MYSQL_DB_DATABASE)%"
    }
  }
}
